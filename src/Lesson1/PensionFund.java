package Lesson1;

public class PensionFund {
    private boolean isGovermental;

    public PensionFund (boolean isGovermental) {
        this.isGovermental = isGovermental;
    }

    private double calculatePensionFor(AbleToCalculatePension) {
        if (isGovermental) {
            return person.calculatePension();
        } else  {
            return 0;
        }
    }
}
